= email_default_heading("Hello, #{@resource.name}!")
%p
  Someone has requested to reset the password for your
  Stoplight account on #{link_to(Gitlab.config.gitlab.url, Gitlab.config.gitlab.url)}.
%p
  Click the link below to complete the process.
#cta
  = link_to('Reset password', edit_password_url(@resource, reset_password_token: @token))
