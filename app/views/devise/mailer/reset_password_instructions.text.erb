Hello, <%= @resource.name %>!

A password reset has been requested for your Stoplight account.

Click the link below to complete the process:

<%= edit_password_url(@resource, reset_password_token: @token) %>
